# .github
RSS Feed Reader keeps subscriptions you own in one named inbox so unread stays easy to reverse on a duty host. FeedReader sits beside that same OPML, while Best Feed Reader stays a search label you can still document each day without extra noise on that named folder each morning.
